# Local dev: SQLite local compartido con apps/api
# (path relativo desde packages/db — ambos apuntan al mismo archivo)
DATABASE_URL=file:../../apps/api/local.db
# DATABASE_AUTH_TOKEN=         # vacío en local

# Prod (Turso) — descomenta y rellena cuando tengas la DB de Turso
# DATABASE_URL=libsql://your-db.turso.io
# DATABASE_AUTH_TOKEN=eyJhbGciOi...
